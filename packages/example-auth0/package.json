{
  "name": "@lambda-edge-authorizers/example-auth0",
  "description": "Protect Cloudfront resources behind authentication services",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "run-s build:*",
    "build:clean": "rimraf ./index.dist.js",
    "build:index": "esbuild index.ts --bundle --platform=node --target=node18 --format=cjs --outfile=index.dist.js"
  },
  "dependencies": {
    "lambda-edge-authorizers": "../lambda-edge-authorizers"
  },
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  }
}
