// import { aesEncrypt, aesDecrypt } from '@someimportantcompany/utils';

// test('should decode a cookie value')

// test('should decode a cookie value with a secret')

// test('should encode a cookie value')

// test('should encode a cookie value with a secret')
